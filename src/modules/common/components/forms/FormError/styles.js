import { makeStyles } from '@material-ui/styles';

 
export const useStyles = makeStyles({
    errorMessage: {
        display: 'flex',
        alignItems: 'center'
    },
    errorIcon: {
        color: '#f22053',
        marginRight: 6,
        fontSize: 20,
    },
});